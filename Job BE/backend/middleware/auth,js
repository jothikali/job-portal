
const verifyToken = (req, res, next) => {
    const token = req.headers['authorization'];
    if(!token) return res.status(403).json("Token required");
    
    // logic to verify token...
    next(); 
};


router.get('/candidates', verifyToken, jobController.getCandidates);